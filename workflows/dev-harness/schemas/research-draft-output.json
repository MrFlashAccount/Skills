{
  "title": "DevHarness Research Draft Output",
  "description": "Producer output contract for the DevHarness research draft gate before critic attack.",
  "type": "object",
  "required": [
    "outcome"
  ],
  "properties": {
    "outcome": {
      "enum": [
        "ready_for_attack",
        "blocked"
      ],
      "description": "Producer must emit the single enum value that reflects the completed stage state; use blocked only when blocker explains the stop condition.",
      "x-usage": "Treat this as the authoritative workflow state for this output. Do not infer success from other populated fields when this says blocked or needs revision."
    },
    "research_packet": {
      "type": "object",
      "required": [
        "summary",
        "scope",
        "constraints",
        "risks",
        "open_questions",
        "recommendation"
      ],
      "properties": {
        "summary": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Producer must summarize the relevant decision or handoff in concise human-readable form without adding new scope.",
          "x-usage": "Use this as concise human-readable context only. Do not route, approve, or expand scope from this text."
        },
        "scope": {
          "type": "object",
          "description": "Producer must describe the task scope, including in-scope and out-of-scope boundaries discovered during research.",
          "x-usage": "Use this to understand what is in and out of scope. Do not add excluded or unresolved work to the task."
        },
        "constraints": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Producer must list discovered constraints that apply to later architecture and planning stages.",
          "x-usage": "Carry these constraints into later work unless a newer approved contract explicitly supersedes them."
        },
        "risks": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Producer must list known risks discovered during research with enough specificity for later gate evaluation.",
          "x-usage": "Use these as risk inputs for decisions and handoffs. Do not treat a risk as blocking unless outcome, open questions, or policy makes it blocking."
        },
        "open_questions": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Producer must list unresolved questions that may block progress or lower confidence.",
          "x-usage": "Resolve or explicitly accept these before relying on the research as final. Do not infer answers from the recommendation."
        },
        "recommendation": {
          "type": "string",
          "minLength": 1,
          "description": "Producer must state the researcher recommendation for the next gate, approval decision, or plan direction.",
          "x-usage": "Treat this as advisory research judgment. Do not let it approve implementation, freeze scope, or override constraints/blockers."
        }
      },
      "additionalProperties": true,
      "description": "Producer must provide the discovery packet for the task, including scope, constraints, risks, open questions, and recommendation.",
      "x-usage": "Treat this as the discovery baseline only after the required approval/pass outcome. Use it for scope and evidence; do not treat it as permission to implement."
    },
    "artifacts": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/artifact"
      },
      "description": "Producer must emit the research packet artifact metadata when outcome is ready_for_attack.",
      "x-usage": "The research packet markdown is the source of truth for human approval. Use id + path; ref is optional/derived and should not be required for lookup.",
      "minItems": 1
    },
    "results": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/result"
      },
      "description": "Producer may emit compact structured results, decisions, or evidence summaries created by this stage.",
      "x-usage": "Use these as supporting evidence or compact summaries. Do not let them override outcome, selected steps, blockers, or approved contracts."
    },
    "blocker": {
      "type": "object",
      "required": [
        "summary",
        "source_step_id",
        "needed"
      ],
      "properties": {
        "summary": {
          "type": "string",
          "minLength": 1,
          "description": "Producer must summarize the relevant decision or handoff in concise human-readable form without adding new scope.",
          "x-usage": "Use this as concise human-readable context only. Do not route, approve, or expand scope from this text."
        },
        "source_step_id": {
          "type": "string",
          "minLength": 1,
          "description": "Producer must name the workflow step id that found or owns this blocker or finding.",
          "x-usage": "Use this to identify the step that found or owns the blocker/finding. Do not treat it as a retry target by itself."
        },
        "needed": {
          "type": "string",
          "minLength": 1,
          "description": "Producer must state the exact missing decision, permission, input, or evidence required to proceed.",
          "x-usage": "Surface this exact missing input, permission, evidence, or decision before proceeding. Do not substitute assumptions for it."
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Producer should list concrete evidence references supporting this handoff, verdict, finding, or blocker.",
          "x-usage": "Use these references to inspect or challenge the claim. Do not assume unreferenced surfaces were checked."
        },
        "risk": {
          "type": "string",
          "description": "Producer should describe the residual risk if this blocker or finding is ignored.",
          "x-usage": "Use this to understand why ignoring the item is unsafe. Treat it as risk context unless paired with a blocked outcome."
        }
      },
      "additionalProperties": false,
      "description": "Producer must populate this when outcome is blocked, naming the stop reason, owning step, and specific missing input or permission.",
      "x-usage": "Treat this as a stop condition. Surface the missing input or decision instead of continuing or working around it."
    }
  },
  "additionalProperties": false,
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/MrFlashAccount/Skills/schemas/workflow/dev-harness/research-draft-output",
  "allOf": [
    {
      "if": {
        "properties": {
          "outcome": {
            "const": "ready_for_attack"
          }
        },
        "required": [
          "outcome"
        ]
      },
      "then": {
        "required": [
          "research_packet",
          "artifacts"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "outcome": {
            "const": "blocked"
          }
        },
        "required": [
          "outcome"
        ]
      },
      "then": {
        "required": [
          "blocker"
        ]
      }
    }
  ]
}
