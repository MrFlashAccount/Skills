{
  "name": "skills-dev-harness-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "make -C develop validate",
    "maintain:bundle-schema-validation": "node scripts/bundle-schema-validation.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "esbuild": "^0.28.0"
  },
  "workspaces": [
    "shared/scripts/schema-validation"
  ]
}
