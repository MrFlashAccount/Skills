{
  "name": "skills-dev-harness-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "make -C develop validate",
    "maintain:bundle-json-schema-validation": "node scripts/bundle-json-schema-validation.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "esbuild": "^0.28.0"
  }
}
