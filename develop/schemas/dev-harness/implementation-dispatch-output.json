{
  "title": "DevHarness Implementation Dispatch Output",
  "description": "Output contract that turns the approved plan route into concrete implementation step targets.",
  "type": "object",
  "required": [
    "outcome",
    "verdict"
  ],
  "properties": {
    "outcome": {
      "enum": [
        "backend",
        "frontend",
        "architecture",
        "backend_frontend",
        "backend_architecture",
        "frontend_architecture",
        "all",
        "blocked"
      ],
      "description": "Machine-readable stage outcome used by workflow routing.",
      "x-usage": "Downstream next/match logic reads this value; choose only the enum/const value that matches the stage state."
    },
    "verdict": {
      "type": "object",
      "required": [
        "summary",
        "selected_implementation_steps"
      ],
      "properties": {
        "summary": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Concise human-readable summary for this contract section."
        },
        "selected_implementation_steps": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "enum": [
              "backend_implementation",
              "frontend_implementation",
              "architecture_artifact_update"
            ]
          },
          "description": "Concrete implementation branch step ids selected for this run.",
          "x-usage": "Runtime dispatch reads these step ids as the implementation fan-out target."
        }
      },
      "additionalProperties": true,
      "description": "Structured decision payload for this gate.",
      "x-usage": "Downstream prompts use this as the authoritative critique/review result."
    },
    "blocker": {
      "type": "object",
      "required": [
        "summary",
        "source_step_id",
        "needed"
      ],
      "properties": {
        "summary": {
          "type": "string",
          "minLength": 1,
          "description": "Concise human-readable summary for this contract section."
        },
        "source_step_id": {
          "type": "string",
          "minLength": 1,
          "description": "Workflow step id that discovered or owns the blocker."
        },
        "needed": {
          "type": "string",
          "minLength": 1,
          "description": "Specific missing input, decision, permission, or evidence needed to proceed."
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Evidence references supporting the handoff, verdict, or blocker."
        },
        "risk": {
          "type": "string",
          "description": "Residual risk if the blocker or finding is ignored."
        }
      },
      "additionalProperties": false,
      "description": "Blocker details required when the stage cannot produce a usable contract.",
      "x-usage": "Populate when outcome is blocked so the orchestrator can stop and ask for the missing input or decision."
    }
  },
  "additionalProperties": false,
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/MrFlashAccount/Skills/schemas/workflow/dev-harness/implementation-dispatch-output"
}
