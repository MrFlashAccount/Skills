{
  "title": "DevHarness Research Draft Output",
  "description": "Output contract for the DevHarness research draft packet before critic attack.",
  "type": "object",
  "required": [
    "outcome",
    "research_packet"
  ],
  "properties": {
    "outcome": {
      "enum": [
        "ready_for_attack",
        "blocked"
      ],
      "description": "Machine-readable stage outcome used by workflow routing.",
      "x-usage": "Downstream next/match logic reads this value; choose only the enum/const value that matches the stage state."
    },
    "research_packet": {
      "type": "object",
      "required": [
        "summary",
        "scope",
        "constraints",
        "risks",
        "open_questions",
        "recommendation"
      ],
      "properties": {
        "summary": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Concise human-readable summary for this contract section."
        },
        "scope": {
          "type": "object",
          "description": "Structured description of in-scope and out-of-scope work."
        },
        "constraints": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Constraints the downstream architecture and planning stages must obey."
        },
        "risks": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Known risks discovered during research."
        },
        "open_questions": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Unresolved questions that may block or limit confidence."
        },
        "recommendation": {
          "type": "string",
          "minLength": 1,
          "description": "Researcher recommendation for the next gate or plan."
        }
      },
      "additionalProperties": true,
      "description": "Research packet that frames scope, constraints, risks, open questions, and recommendation.",
      "x-usage": "Attack and planning steps use this as the approved discovery baseline."
    },
    "artifacts": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/artifact"
      },
      "description": "Optional structured artifacts emitted by this stage.",
      "x-usage": "Use for named files or durable handoff artifacts the orchestrator should preserve."
    },
    "results": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/result"
      },
      "description": "Optional structured result summaries emitted by this stage.",
      "x-usage": "Use for compact machine-readable evidence or decisions that should remain in baton state."
    },
    "blocker": {
      "type": "object",
      "required": [
        "summary",
        "source_step_id",
        "needed"
      ],
      "properties": {
        "summary": {
          "type": "string",
          "minLength": 1,
          "description": "Concise human-readable summary for this contract section."
        },
        "source_step_id": {
          "type": "string",
          "minLength": 1,
          "description": "Workflow step id that discovered or owns the blocker."
        },
        "needed": {
          "type": "string",
          "minLength": 1,
          "description": "Specific missing input, decision, permission, or evidence needed to proceed."
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Evidence references supporting the handoff, verdict, or blocker."
        },
        "risk": {
          "type": "string",
          "description": "Residual risk if the blocker or finding is ignored."
        }
      },
      "additionalProperties": false,
      "description": "Blocker details required when the stage cannot produce a usable contract.",
      "x-usage": "Populate when outcome is blocked so the orchestrator can stop and ask for the missing input or decision."
    }
  },
  "additionalProperties": false,
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/MrFlashAccount/Skills/schemas/workflow/dev-harness/research-draft-output"
}
